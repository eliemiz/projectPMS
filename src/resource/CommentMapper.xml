<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pms.a03_dao.A19_CommentDao">
	<!-- 댓글 등록 -->
	<insert id="insComment" parameterType="comment">
		INSERT INTO COMMENTS VALUES (COMMENTS_SEQ,#{document_type},#{document_id},
			#{account_id},#{content},#{created_on},#{updated_on})
	</insert>
	<!-- 댓글 조회 -->
	<select id="CommentInfo" resultType="comment" parameterType="int">
		select * from comments
		where document_id = #{document_id}
	</select>
	<!-- 댓글 수정 -->
	<update id="updateComment" parameterType="hashmap">
		update comments
			set content = #{content}
		where id = #{id}
	</update>
	<!-- 댓글 삭제 -->
	<delete id="deleteComment" parameterType="int">
		delete from comments where id = #{id}
	</delete>
</mapper>